fix(api): Add debug logging for authentication to diagnose 401 errors

P0 Fix: Add detailed logging to identify why authentication is failing

Changes:
- Add debug logging for authHeader and cronSecret (values masked for security)
- Check both req.headers.authorization and req.headers.Authorization
- Improve error message to include more context
- Log user-agent to identify if request is from Vercel Cron or manual test

This will help identify:
1. Whether authHeader is being received
2. Whether CRON_SECRET is set in environment
3. Whether the Authorization header format is correct
4. Whether the request is from Vercel Cron or manual test

Expected result: Runtime logs will show detailed authentication information to diagnose 401 errors.
