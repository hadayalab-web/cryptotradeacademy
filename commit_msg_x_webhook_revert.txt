revert: X Webhookを以前の動作していたバージョンに戻す

- api/x-webhook.js: generateCrcResponse関数を元の実装に戻す（throw new Error）
- api/x-webhook.js: logWebhookAccessをawaitに戻す（以前の動作に復元）

変更内容:
1. generateCrcResponse: nullを返すのではなく、エラーをスローするように戻す
2. logWebhookAccess: 非同期実行ではなく、awaitで実行するように戻す

これにより、以前正常に動作していたバージョンに戻り、XのWebhookが正常に動作するようになる。
