@github-copilot @github-copilot-actions

**URGENT: All Vercel Deployments Failing - Root Cause Analysis Required**

すべてのデプロイメントが失敗しています。12時間以上の継続的なエラー状態です。

**Error Pattern**:
- すべてのデプロイが "Error" ステータス
- 最新5件すべて失敗（2m ago, 3m ago, 6m ago, 10m ago, 11m ago）
- エラー内容: `Cannot find module '../config/marketProfiles'` (推測)

**Attempted Solutions (All Failed)**:
1. ❌ `includeFiles: "config/**"` (string syntax error)
2. ❌ `includeFiles: ["config/**"]` (array format)
3. ❌ `includeFiles: ["config/**/*", "config/*"]` (explicit patterns)
4. ❌ Build script copying `config/` to `api/config/` (current approach)

**Current Implementation**:
- `vercel.json`: `buildCommand: "npm run build"`
- `scripts/copy-config.js`: Copies `config/` to `api/config/`
- `services/grok/client.js`: Fallback path resolution

**Critical Questions**:
1. Is the build script actually running on Vercel?
2. Are copied files accessible at runtime in serverless functions?
3. Is there a better approach for including non-standard directories?
4. Should we commit `api/config/` directly to Git instead?

**Request**:
Please analyze the latest Vercel error logs, identify the root cause, and implement a working solution. This has been blocking deployments for 12+ hours.




