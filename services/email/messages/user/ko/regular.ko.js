/**
 * Phase 4: Email Regular Briefing (KO)
 * Snapshot-native. Delegates to EN implementation (shared HTML structure).
 */
const { formatRegularBriefingHTML } = require('../en/regular.en');
module.exports = { formatRegularBriefingHTML };
