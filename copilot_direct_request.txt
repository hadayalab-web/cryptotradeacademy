@github-copilot @github-copilot-actions

Please investigate and fix the Vercel deployment error in this PR.

**ERROR**: `Cannot find module '../config/marketProfiles'`

**Problem**: The `includeFiles` configuration in `vercel.json` is not working correctly. The `config/` folder is not being included in the deployment bundle.

**Dependency chain**:
```
api/cron.js 
  → require('../services/grok/client')
    → require('../config/marketProfiles')  ← FAILING HERE
```

**Current vercel.json**:
```json
{
  "functions": {
    "api/cron.js": {
      "includeFiles": ["config/**/*", "config/*"]
    }
  }
}
```

**Attempted fixes (all failed)**:
1. `"includeFiles": "config/**"` (string)
2. `"includeFiles": ["config/**"]` (array)
3. `"includeFiles": ["config/**/*", "config/*"]` (explicit patterns)

**What I need**:
1. Root cause analysis - why `includeFiles` is not working
2. Alternative solution (build script, file structure change, etc.)
3. Implementation of the fix

**Files to review**:
- `vercel.json`
- `api/cron.js`
- `services/grok/client.js` (line 4: `require('../config/marketProfiles')`)
- `config/marketProfiles.js`

Please investigate and provide a working solution.

