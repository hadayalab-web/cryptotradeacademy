fix(api,services): Fix p-limit ES Module import errors in all files

P0 Fix: HTTP 500 errors caused by require() of ES Module

Root Cause:
- p-limit is an ES Module but was being imported with require() in multiple files
- Error: [ERR_REQUIRE_ESM]: require() of ES Module /var/task/node_modules/p-limit/index.js not supported

Files Fixed:
1. api/x-quote-repost.js
   - Changed require('p-limit') to dynamic import()
   - Added getPLimit() helper function for lazy loading

2. services/cryptoquant/client.js
   - Changed require('p-limit') to dynamic import()
   - Added getPLimit() helper function
   - Changed rateLimitQueue initialization to async getRateLimitQueue()
   - Updated rateLimitQueue usage to await getRateLimitQueue()

Impact:
- Fixes HTTP 500 errors for all x-quote-repost-{lang} endpoints
- Fixes potential errors in CryptoQuant client
- All 6 language endpoints should now work correctly

Reference:
- Error logs showed 171 HTTP 500 errors, all with same p-limit import error
- Pattern matches prepare.js p-retry implementation
