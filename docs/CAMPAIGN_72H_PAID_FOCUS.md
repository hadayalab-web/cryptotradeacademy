# 48h 有料集中キャンペーン：修正案

**※ 以下「承認済み方針」は参謀推奨案をすべて承認したうえでの確定方針とする。**

---

## 承認済み方針（参謀推奨 → 全承認）

### 2導線のランチェスター（主・従）

| 役割 | 導線 | 割合・用途 |
|------|------|------------|
| **主** | Vidalytics → Whop（Regular） | リプ CTA の **8〜9割**。ストーリー→VSL→オファーで納得して申し込む層。主 KPI。 |
| **従** | Whop 直（Regular） | **1〜2割**。「もう話は聞いた／リンクだけ」用の短い導線。サブ KPI。 |

- 計測: Vidalytics 経由の成約を主、Whop 直の成約をサブで見る。

### 緊急性・希少性フォーマット

- **採用フォーマット**: **「48h 限定」** ＋ 枠（先着50名／あと〇枠）。「48h限定・先着50名でこのリンクは閉じる」「あと〇枠・48h限定」で統一（実装済み: getScarcityLine）。

---

## 方針（3本柱）

1. **有料導線に絞る**
2. **ローリング希少性**（先着50名 → 完売したら追加50名を宣言して繰り返し）
3. **リプライ絶対数を増やす**

---

## 1. 有料導線に絞る

### 対象にする導線（2本のみ・主従あり）

| 役割 | 導線 | 経路 | 成果物 |
|------|------|------|--------|
| **主** | A | クリック → **Vidalytics VSL** → Whop **Regular** | 有料版（Regular Briefing） |
| **従** | B | クリック → **Whop 直** **Regular** | 有料版（Regular Briefing） |

- **Minimal（無料版）** への誘導は、キャンペーン期間中は出さない or 控えめにする。
- リプの CTA: **主（Vidalytics）を 8〜9割**、**従（Whop 直）を 1〜2割**。従は「すぐ試す用」など用途を限定。

### 実装まわりでやること

- **pqtTemplates / pqtCtaEngine**: キャンペーン用フラグ or 環境変数で「有料（Regular）のみ」に切り替え。
- リンクは **Vidalytics（Regular 着地）** と **Whop 直（Regular）** のみ使用。Minimal 用リンクは出さない。

---

## 2. ローリング希少性（48h限定フォーマット・先着50名 → 追加50名でループ）

- **採用理由**: 先着50名が完売しても「さらに先着50名分を追加」と宣言すれば終了で詰まらない。同じ枠を繰り返せるので運用が破綻しない。
- **48h 限定フォーマット**: 心理的効果を優先し、**「48h 限定」** を枠とセットで統一。「48h限定・先着50名でこのリンクは閉じる」「あと〇枠・48h限定」でリプ・LP・VSL を揃える。
- **「あと〇枠」**: 現在のバッチで残りが減るたびに「あと12枠・48h限定」「あと3枠・48h限定」と締められる。売り切れたら追加50枠でリセットするので、**「あと何枠」は永遠に使い回せる**。
- **トレードオフ**: 同じフォロワーが何度も「追加50名」を見ると「実質無制限では？」と気づくリスクはあるが、**投資対効果を優先**する方針で許容する。リプの届き先は毎回違うスレ・ユーザーが中心なので、多くの人には「今の50枠・48h」として機能する。

### 運用

- 先着50名を宣言 → 完売（または区切りが良くなったタイミング）で「追加50名分を開放」と更新。必要に応じて繰り返す。
- 文言は **「48h限定・先着50名」**「あと〇枠・48h限定」で統一。追加時は「ご要望多かったため追加50名分・48h限定」などで自然に繋ぐ。

### 緊急性・希少性の入れどころ（承認済み: 48h限定＋枠）

| 場所 | 修正案 |
|------|--------|
| **リプライ本文** | **「48h限定・先着50名でこのリンクは閉じる」**「あと〇枠・48h限定」。実装済み（getScarcityLine）。手堅いトーンは維持。 |
| **Whop LP** | DEFEND50 の近くに「48h限定・先着50名まで」を明記。追加開放時は LP 文言を更新。 |
| **Whop LP VSL** | 「48h only — next 50 people」→ 完売後に「We're opening 50 more spots. 48h only.」などに差し替え可能。 |
| **Vidalytics VSL**（別導線用） | 同様に「48h only」＋「next 50」／「50 more spots」でローリング。 |

### オファー

- **DEFEND50**（50% off）はそのまま。
- プラン: 月額 $99（1日トライアル）・3ヶ月 $237（20% off）・年額 $845（29% off）。クーポンで実質半額。

### 文言の例（リプ用・48h限定フォーマット）

- 「48h限定・先着50名でこのリンクは閉じる。止まる→確認→条件一致で動く。」
- あと〇枠時: 「あと12枠・48h限定。構造だけ確認しておくと差がつく。」
- 追加開放後: 「追加50枠が開放された・48h限定。構造だけ確認しておくと差がつく。」

---

## 3. リプライ絶対数を増やす

### 解像度は変えず、Run 頻度を上げる

- **現状**: `BUZZWEAVE_MIN_RUN_INTERVAL_HOURS=3` → 1日 **8 Run**（Cron も 3h 間隔）。
- **キャンペーン中**: 間隔を **1時間** に変更 → 1日 **24 Run**。理論上 **約3倍** のスキャン機会。
- 選定基準（高解像度スキャン・有力仕手Bot・impression×hype×copyFit）は **そのまま**。乗せる先の質は落とさない。

### 実装済み（環境変数で制御）

- **Cron**: `vercel.json` で **毎時 0 分**（`0 * * * *`）に変更済み。通常時は間隔 3h で実質 8 Run/日、キャンペーン時は 1h で最大 24 Run/日。
- **BUZZWEAVE_MIN_RUN_INTERVAL_HOURS**: 未設定時、`CAMPAIGN_PAID_FOCUS=true` なら **1**、それ以外は **3**（`api/buzzweave-run.js`）。
- **日次 Run 上限**: `CAMPAIGN_PAID_FOCUS=true` のとき `determineDailyRunTarget` が **24** を返す（`BUZZWEAVE_DAILY_RUN_CAMPAIGN` で上書き可）。通常時は従来どおり 6/7/8。
- **cap（1 Run あたりのリプ数）**: 現状の cap のまま。Run 回数増で総リプ数が増える。

### キャンペーン終了後

- **CAMPAIGN_PAID_FOCUS** を外す or `false` にすると、間隔 3h・日次上限 8 の挙動に戻る（Cron は毎時のまま。間隔チェックで 3h に 1 回だけ実行される）。

---

## 環境変数（キャンペーン用）

| 変数 | 効果 |
|------|------|
| **CAMPAIGN_PAID_FOCUS** | `true` or `1` で有料2導線のみ・主従比率 85/15・Run 1h・日次上限 24・希少性リプ追加を有効化。 |
| **CAMPAIGN_SLOTS_LEFT** | 数値で「あと〇枠・48h限定」をリプに追加（未設定時は「48h限定・先着50名でこのリンクは閉じる」）。ローリングで枠を更新するときに設定。 |
| **CAMPAIGN_SCARCITY_VARIANT** | 希少性コピーの出し分け。`fear`（恐怖訴求）／`authority`（権威訴求）／`elitism`（選民意識）。未設定時はデフォルトの「48h限定・先着50名」を使用。 |
| **BUZZWEAVE_DAILY_RUN_CAMPAIGN** | キャンペーン時の日次 Run 上限（デフォルト 24）。必要なら下げて利用。 |

## 運用チェックリスト（キャンペーン開始前）

- [x] 有料2導線（Vidalytics 主 / Whop直 従）・主従 85/15 は `CAMPAIGN_PAID_FOCUS=true` で有効（実装済み）
- [x] リプに「48h限定・先着50名」／「あと〇枠・48h限定」は `getScarcityLine` で追加（実装済み）。LP・VSL は手動で「48h限定」を入れる
- [x] Run 間隔 1h・Cron 毎時・日次上限 24 は `CAMPAIGN_PAID_FOCUS=true` で有効（実装済み）
- [ ] DEFEND50 が Whop で有効であることを確認
- [ ] キャンペーン終了後は `CAMPAIGN_PAID_FOCUS` を外す（間隔・上限は自動で通常に戻る）

---

## 参照

- 導線・CVR 想定: `docs/FUNNEL_CVR_AND_CV_ESTIMATE.md`
- BuzzWeave Run ロジック: `docs/BUZZWEAVE_RUN_LOGIC.md`
- リプライテンプレ: `services/td/pqtTemplates.js`（PROMO_LINE, DEFEND50, 緊急・希少用の追加案）
- CTA 組み立て: `services/td/pqtCtaEngine.js`
