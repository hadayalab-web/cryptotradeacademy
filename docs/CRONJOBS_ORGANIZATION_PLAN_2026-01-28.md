# CronJobs整理計画
**作成日時**: 2026-01-28  
**目的**: X投稿関連とTG DM関連のCronJobsを整理し、保守性と一貫性を向上させる

---

## 📊 現状分析

### X投稿関連（4件）

| CronJob | スケジュール | 実行頻度 | 機能概要 | 状態 |
|---------|------------|---------|---------|------|
| `/api/vsl1-post` | `0 1,13,21 * * *` | 1日3回 | VSL1自動投稿（X/Twitterのみ） | ✅ 正常 |
| `/api/x-post-minimal-version-cron` | `0 0,7,12,15,23 * * *` | 1日5回 | 無料版（Minimal Version）のX投稿 | ✅ 正常 |
| `/api/x-post-free-report` | `30 4,10,17,19 * * *` | 1日4回 | 無料版レポートX投稿（6言語対応） | ✅ 正常 |
| `/api/x-quote-repost` | `0 * * * *` | 1時間ごと | 引用リポスト自動化（386人ローテーション） | ✅ 正常 |

**合計**: 1日約500前後投稿（引用リポスト含む）

---

### TG DM関連（3件）

| CronJob | スケジュール | 実行頻度 | 機能概要 | 状態 |
|---------|------------|---------|---------|------|
| `/api/vsl2-free-users` | `0 * * * *` | 1時間ごと | VSL2自動配信（24時間後） | ✅ 正常 |
| `/api/vsl1-reminder` | `0 */12 * * *` | 12時間ごと | VSL1リマインド（12時間後） | ✅ 正常 |
| `/api/vsl2-last-call` | `0 * * * *` | 1時間ごと | VSL2ラストコール（21時間後） | ✅ 正常 |

**配信タイミング**:
- 12時間後: VSL1リマインド
- 21時間後: VSL2ラストコール
- 24時間後: VSL2配信

---

## 🔍 整理のポイント

### 1. コードの重複確認

#### X投稿関連
- **共通機能**: 
  - X投稿（`postTweet`, `postQuoteTweet`）
  - メディアアップロード（`uploadMedia`）
  - コンフィグチェック（`getXConfigStatus`）
  - 言語正規化（`normalizeLang`）
- **重複コード**: 
  - 言語正規化ロジック（各ファイルに個別実装）
  - Whop URL取得ロジック（一部重複）
  - エラーハンドリングパターン

#### TG DM関連
- **共通機能**:
  - Telegram DM送信（`sendMessageToUser`, `sendPhotoToUser`）
  - 無料ユーザー取得（`getFreeUsersForVSL2`, `getFreeUsersForVSL1Reminder`等）
  - 言語正規化
  - VSLリンク検証
- **重複コード**:
  - VSLリンク検証ロジック（VSL1/VSL2混同防止）
  - Whop URLマッピング（3ファイルで同一実装）
  - 言語正規化ロジック

---

### 2. スケジュール最適化の検討

#### X投稿関連
- **現状**: 合計12回/日（VSL1: 3回、Minimal: 5回、Free Report: 4回）+ 引用リポスト（24回/日）
- **検討事項**: 
  - 時間帯の重複チェック（例: `x-post-minimal-version-cron` と `x-post-free-report` が近い時間帯）
  - ピーク時間帯の最適化（`services/x/optimization.js`の`isPeakHourForLang`を活用）

#### TG DM関連
- **現状**: 
  - `vsl2-free-users`: 1時間ごと（24時間後チェック）
  - `vsl1-reminder`: 12時間ごと（12時間後チェック）
  - `vsl2-last-call`: 1時間ごと（21時間後チェック）
- **検討事項**: 
  - 実行頻度の最適化（1時間ごとで十分か？）
  - バッチ処理の効率化

---

### 3. 共通化の提案

#### 共通ユーティリティの作成

**`utils/vsl-common.js`** (新規作成)
```javascript
// VSL関連の共通ロジック
- VSLリンク検証（VSL1/VSL2混同防止）
- Whop URL取得（言語別）
- 言語正規化（VSL専用）
```

**`utils/x-posting-common.js`** (新規作成)
```javascript
// X投稿関連の共通ロジック
- X投稿前のコンフィグチェック
- エラーハンドリング統一
- ログフォーマット統一
```

---

## 📋 整理タスク

### Phase 1: 共通ユーティリティの作成

- [ ] `utils/vsl-common.js` を作成
  - VSLリンク検証関数
  - Whop URL取得関数
  - 言語正規化関数（VSL専用）
- [ ] `utils/x-posting-common.js` を作成
  - X投稿前チェック関数
  - エラーハンドリング統一
  - ログフォーマット統一

### Phase 2: コードリファクタリング

#### X投稿関連
- [ ] `api/vsl1-post.js` を共通ユーティリティ使用に変更
- [ ] `api/x-post-minimal-version-cron.js` を共通ユーティリティ使用に変更
- [ ] `api/x-post-free-report.js` を共通ユーティリティ使用に変更
- [ ] `api/x-quote-repost.js` を共通ユーティリティ使用に変更

#### TG DM関連
- [ ] `api/vsl2-free-users.js` を共通ユーティリティ使用に変更
- [ ] `api/vsl1-reminder.js` を共通ユーティリティ使用に変更
- [ ] `api/vsl2-last-call.js` を共通ユーティリティ使用に変更

### Phase 3: ドキュメント整備

- [ ] `docs/X_POSTING_CRONJOBS.md` を作成（X投稿関連の詳細ドキュメント）
- [ ] `docs/TG_DM_CRONJOBS.md` を作成（TG DM関連の詳細ドキュメント）
- [ ] `vercel.json` のコメントを更新（各CronJobの説明を追加）

### Phase 4: スケジュール最適化（オプション）

- [ ] X投稿の時間帯重複をチェック
- [ ] TG DMの実行頻度を最適化（必要に応じて）

---

## 🎯 期待される効果

1. **保守性向上**: 共通ロジックの一元管理により、変更時の影響範囲を最小化
2. **一貫性確保**: エラーハンドリング、ログフォーマット、言語処理の統一
3. **コード削減**: 重複コードの削減により、コードベースのサイズを削減
4. **バグ防止**: VSLリンク混同などの既知の問題を共通関数で防止

---

## ⚠️ 注意事項

1. **既存機能への影響**: リファクタリング時は既存の動作を維持すること
2. **テスト**: 各CronJobの動作確認を実施すること
3. **段階的実装**: Phase 1 → Phase 2 → Phase 3 の順で段階的に実装すること
4. **ロールバック準備**: 問題発生時に即座にロールバックできるよう準備すること

---

## 📝 次のステップ

1. ユーザーと整理方針を確認
2. Phase 1から順次実装開始
3. 各Phase完了時に動作確認
4. 完了後にドキュメント更新
